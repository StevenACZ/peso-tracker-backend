# 🏗️ PESO TRACKER - Environment Variables Template
# Copia este archivo y renómbralo según tu entorno:
# - Para desarrollo local: usa .env.development (ya existe)
# - Para VPS producción: crea .env.production con estos valores

# === CONFIGURACIÓN BÁSICA ===
NODE_ENV=production
PORT=3000

# === SEGURIDAD ===
# ⚠️ IMPORTANTE: Cambia estos valores en producción
JWT_SECRET=tu-jwt-secret-ultra-seguro-de-al-menos-32-caracteres-aqui
JWT_EXPIRES_IN=24h
BCRYPT_SALT_ROUNDS=12

# === CORS ===
# URL de tu frontend (React, Vue, etc.)
FRONTEND_URL=https://tu-frontend.tudominio.com

# === BASE DE DATOS ===
# PostgreSQL en tu VPS (interno, sin SSL)
DATABASE_URL=postgresql://tu_usuario:tu_password@localhost:5432/peso_tracker
DIRECT_URL=postgresql://tu_usuario:tu_password@localhost:5432/peso_tracker

# === ALMACENAMIENTO DE ARCHIVOS ===
# Paths en tu VPS
UPLOADS_PATH=/var/www/peso-tracker/uploads
BASE_URL=https://api.tudominio.com
MAX_FILE_SIZE=5242880

# === RATE LIMITING ===
# Más restrictivo en producción
RATE_LIMIT_TTL=60
RATE_LIMIT_LIMIT=100

# === DATABASE RESET ===
# ⚠️ NUNCA pongas true en producción
RESET_DATABASE=false