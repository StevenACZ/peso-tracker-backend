meta {
  name: 05 - Delete Weight
  type: http
  seq: 6
}

delete {
  url: {{baseUrl}}/weights/{{lastWeightId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

tests {
  test("Weight deleted successfully", function() {
    expect(res.status).to.equal(200);
    expect(res.body.message).to.include("eliminado");
  });
}
